# SailViz 機能ロードマップ / バックログ

追加したい機能をここに貯めて、上から順に実装していく。
各機能は「短い設計 → 承認 → 実装 → テスト」で回す。

## ステータス凡例
- 💡 アイデア（未整理）
- 📝 設計中
- 🚧 実装中
- ✅ 完了
- ❄️ 保留 / 見送り

---

## バックログ

<!--
各機能はこのテンプレで追記する:

### <機能名>
- **ステータス**: 💡
- **カテゴリ**: 解析 / 可視化 / 操作 / データ
- **概要**: 何をするか一言
- **動機**: なぜ欲しいか
- **メモ**: 設計時の論点・依存
-->

### 1. マーク配置（右クリックでコース標識をプロット）
- **ステータス**: ✅ 完了（unproject + 右クリック4択メニュー + マークレイヤー描画 + サイドバー削除）
- **カテゴリ**: 可視化 / 操作
- **概要**: ステージを右クリック→選択ダイアログ→三角/丸（赤・黄）のマークを配置。セーリングのコースマーク（回航ブイ）を軌跡上に置く。
- **動機**: セーリングはマークの周りを周回する競技。基準となるマーク位置を可視化したい。
- **メモ**: 形状(三角/丸)×色(赤/黄)。配置位置の保持方法（lat/lon）。既存タグピンと描画レイヤーを分けるか。編集/削除。

### 2. 時間範囲での軌跡フィルタ表示
- **ステータス**: ✅ 完了（軌跡を単クリック→始点/終点で既存クロップを設定。範囲外のみ点線(α0.5)・範囲内のみ実線で描画＝重複描画なしで破線が隠れない。下バーに青帯＋始点は琥珀の破線プレビュー。Escで取消。全トラック共通）
- **既知の注意**: 破線は「選択範囲の外」にだけ出る。初期状態（範囲未選択＝全体が選択）は実線のみで正常。ループするコースでは選択部と非選択部が同じ水面に重なる点は仕様上不可避。
- **カテゴリ**: 操作 / 可視化
- **概要**: 「この時刻〜この時刻」の区間だけ軌跡を表示。下部バーでの範囲指定、または軌跡上の点クリックで始点/終点入力。
- **動機**: 複数軌跡が重なって見づらい。注目区間だけ抜き出したい。
- **メモ**: 既存のクロップ機能との差別化（クロップは再生範囲も絞る）。表示専用フィルタか、クロップ拡張か。軌跡クリック→時刻取得は `unproject` が要りそう（作りかけの `unproject` と接続？）。

### 3. 現在地マーカーを目立たせる
- **ステータス**: ✅ 完了（renderer.js: 半径9 + 白内輪郭 + 黒外輪郭の二重リング）
- **カテゴリ**: 可視化
- **概要**: 再生中の現在位置の丸を大きく・黒輪郭付きにして視認性を上げる。
- **動機**: 現状の丸が小さくて見づらい。
- **メモ**: `renderer.js` の現在地描画のみの軽微改修。クイックウィン。

### 5. 動画↔GPS現在位置の同期
- **ステータス**: ✅ 完了（動画パネルを開くとその動画がmaster。appクロックを止め、動画のcurrentTimeを絶対時刻に直して毎フレーム(rAF)＋timeupdate/seekedでplayheadをseek→GPS現在位置マーカーが動画に追従。閉じると解除）
- **既知の注意**: playback.seekはcrop(選択範囲)にクランプされるため、狭い区間を選択中は範囲端でマーカーが止まる。全体を追う場合は区間選択を解除（下バーで全体に戻す）。将来: 動画master時はクランプ回避も検討。

### 7. 動画範囲をタイムラインに可視化
- **ステータス**: ✅ 完了（各動画を [開始, 開始+duration] の区間としてタイムライン上端に濃紺バー表示。動画バッジと同色。durationはmvhd優先、無ければ<video>のloadedmetadataから補完。長さ不明時は▶ティック。絶対/経過モード両対応）

### 6. 動画の埋め込み撮影時刻で自動アンカー
- **ステータス**: ✅ 完了（src/videometa.js: moov>mvhd の creation_time(1904起点)＋duration をパース→Unix ms。creation_timeは端末により「録画終了時刻」なので 開始=creation−duration で復元。addVideoで優先採用、取れない/軌跡範囲外なら現在の再生位置にフォールバック。TDDで単体テスト）
- **動機**: Google Drive等でDLするとFSの作成日が書き換わる。埋め込みメタは中身なので保持される。
- **実測メモ**: 利用端末は creation_time=録画終了時刻だった → duration を引いて開始時刻に補正済み。
- **既知の注意**: creation_timeはUTC想定だが端末により現地時刻で書かれ得る。JST(+9h)で書かれた動画は9時間ズレて軌跡範囲外→現在位置にフォールバックしがち。将来: tzオフセット指定 or 近傍探索。

### 4. 動画コメントアイコン（軌跡上の動画開始時点に配置→クリックで再生）
- **ステータス**: ✅ 完了（動画D&D→その瞬間のplayheadを絶対時刻に紐付け。開始時刻に当たる軌跡点へ再生バッジ描画。バッジ/サイドバークリックで右半分に動画パネル(左半分に軌跡)、×/Escで閉じ、キャンバスは再フィット。削除でObjectURL解放）
- **カテゴリ**: データ / 操作
- **概要**: 対応時刻の動画が添付されたら、その動画開始時刻に当たる軌跡上の点へコメントアイコンを表示。クリックで動画を再生。
- **動機**: GPS軌跡と実映像を時刻で紐付けて振り返りたい。
- **メモ**: 動画ファイルの与え方（D&D？時刻メタは？手入力？）。再生UI（モーダル/インライン）。軌跡再生との同期は将来課題。

### 8. 反省ノート（@メンション + 動画メンション + 風向自動取得）
- **ステータス**: ✅ 完了（サイドバーで反省テキストを記入/編集/削除→localStorage永続化 `src/reflections.js`。`@` で部員名簿 `src/members.js` の絞り込み候補ポップアップ→選択で人物メンション挿入。反省エディタを開いた状態で動画バッジ/一覧をクリックすると `[動画:名前@位置]` トークンで動画メンション挿入。気象庁アメダス辻堂(46141)の非公式JSONから練習時間帯の風向・風速を自動取得し、失敗時は手入力フォールバック `src/wind.js`）
- **カテゴリ**: データ / 操作
- **概要**: 練習の振り返りメモを人物・動画・風情報と紐付けて残す。
- **動機**: 軌跡/映像だけでなく「誰が」「どんな風で」の文脈を残して振り返りたい。
- **メモ**: アメダスは非公式APIのため直近1〜2日のみ取得可。名簿のフリガナ未確定行あり（要確認）。

### 9. 動画半自動添付（フォルダ走査でGPS範囲と重なる動画を抽出）
- **ステータス**: ✅ 完了（`src/folderimport.js`: FileSystemDirectoryHandle 直下の動画を走査し、埋め込み撮影時刻＋durationが現在のGPS範囲と重なるものだけを候補として抽出。`videoOverlapsRange` で区間重なり判定。時刻不明/範囲外はスキップ。TDDで単体テスト）
- **カテゴリ**: データ / 操作
- **概要**: 動画を1本ずつD&Dせず、フォルダごと指定→その日の軌跡に該当する動画を自動抽出して添付。
- **動機**: 大量の動画から手作業で該当分を探すのが面倒。埋め込み時刻で自動マッチさせたい。
- **メモ**: Drive連携はローカル同期フォルダ読みで代替（大学アカウント制約）。埋め込み時刻の信頼性は項目6の既知の注意に依存。

### 10. 精度フィルタ（低精度GPS点の除外）
- **ステータス**: ✅ 完了・**既定ON**（`horizontalaccuracy > 50m` の点を読込時に除外。チェックボックスは既定チェック済み。変更は次回読込から反映。精度列が無いデータは全点保持）
- **カテゴリ**: データ / 解析
- **概要**: 水平精度の悪いGPS点を軌跡から除外して外れ・ノイズを抑える。
- **動機**: 屋外/機体陰などで精度が落ちた点が軌跡を乱すため、既定で除外して見やすくしたい。
- **メモ**: 外れ値除去(`rejectOutliers`)とは別段の前処理。閾値50mは固定（将来: 可変閾値）。

### 11. 下バー（タイムライン）から動画を再生・シーク
- **ステータス**: ✅ 完了（`timeline.js` に`onVideoClick`＋`videoHitId`を追加＝上端12pxストリップで動画バーをクリック→`openVideoPanel` で該当動画を再生。動画パネル表示中は下バーのスクラブで動画の `currentTime` を直接シーク＝`syncFromVideo` の逆算 `seekVideoToAxisTime`、`seeked` で playhead 追従。cropハンドル操作は優先、ホバーで `cursor:pointer`。`axisVideos` に `id` を付与。TDDでクリック/点動画/ハンドル優先/スクラブ分岐を単体テスト）
- **カテゴリ**: 操作
- **概要**: タイムライン上端の動画バーをクリックで再生開始し、パネル表示中はスクラブで動画位置も操作できる。
- **動機**: 動画への導線を下バーに集約し、GPS再生と一体で操作したい。

### 12. CSVトラック名のリネーム
- **ステータス**: ✅ 完了（サイドバーのトラック名をダブルクリック→インライン入力(`startRenameTrack`)。Enter/フォーカス外れで確定＝trim・空なら元名維持、Escで取消。既存の `serializeProject` の `t.name` で保存に自動反映）
- **カテゴリ**: 操作 / データ
- **概要**: 読み込んだCSVの表示名をサイト内で変更する。
- **動機**: ファイル名のままだと誰の軌跡か分かりにくい。艦・選手名に付け替えたい。

### 13. 動画の90°回転（表示のみ）
- **ステータス**: ✅ 完了（動画パネルの `⟳` ボタンで90°ずつ回転。`videoview.js` の `nextRotation`/`rotatedFitBox` で回転後もステージに `object-fit:contain` でフィット＝90/270は縦横入替。`#video-stage` ラッパ＋絶対配置＋`transform:rotate`。表示のみ・保存しない、動画を開くとリセット、リサイズで再適用。TDDで純関数を単体テスト）
- **カテゴリ**: 操作 / 可視化
- **概要**: 縦持ち/横持ちが混在する動画をパネル内で90°単位で回して見やすくする。
- **動機**: 撮影向きがバラバラな動画を正しい向きで振り返りたい。

### 14. マップの自由回転（スライダーで任意角度）
- **ステータス**: ✅ 完了（`viewport.js` の `worldToScreen`/`screenToWorld`/`pan` を `transform.rot`(ラジアン)込みに変更＝軌跡・マーク・現在地・タグ・動画バッジ・クリック判定に一括反映、`zoomAt` は `screenToWorld` 経由で自動対応。ステージ右上に回転スライダー(0–360°)＋リセット。表示のみ・保存しない。`fitTransform` 後に `rot` 再適用でリサイズでも維持。TDDで回転の round-trip/90°/pan整合を単体テスト）
- **カテゴリ**: 操作 / 可視化
- **概要**: ステージ右上の回転スライダー(0–360°)でマップ全体を任意角度に回す。リセットで北上に戻す。
- **動機**: 風向やコース方位に合わせて見たい向きに回転して振り返りたい。

### 15. 移動速度のGPS同期表示（各艦・m/s）
- **ステータス**: ✅ 完了（`interpolate.js` に `speedAt` を追加＝CSV `speed` 列を線形補間、null区間は隣接点の haversine/dt でフォールバック。`renderer.js` が各可視トラックの現在地マーカー右上に `x.x m/s` を白縁取り＋トラック色で描画。再生・スクラブで自動更新。TDDで補間/フォールバック/範囲外null＋描画を単体テスト）
- **カテゴリ**: 解析 / 可視化
- **概要**: 再生位置(now)の移動速度を各可視トラックの現在地マーカー近くに m/s で表示。再生・スクラブに同期。
- **動機**: 軌跡だけでなく速度を時刻同期で確認し、走りの良し悪しを振り返りたい。

### 16. 反省ノートに数値・天候・振り返り項目を記録
- **ステータス**: ✅ 完了（反省エディタに折りたたみ`<details>`3セクション。①艇セッティング＝`RIG_FIELDS`12項目(船番号/ギア/プリベンド/レーキ/サイド・フォアテンション/プラー/ピークロープ/ブライダル高/ジブリーダー/ジブ・バング引き量)を`type=number`グリッドに動的生成、②天候＝既存の風向/風速をこの節に集約＋波高、③反省内容＝`NOTE_FIELDS`5項目(目標/感じている課題/発見/遅かった要因/速かった要因)のラベル付きtextareaを動的生成。既存の自由記述本文は据え置き。`reflections.js`に`rig`/`waveHeight`/`notes`を追加し`toNum`で空欄→null正規化、`previousRig`で新規時に直前の艇セッティングをプリフィル(天候は引き継がない)。保存/編集とも`createReflection`経由で正規化。純ロジックはTDDで単体テスト、UIは手動確認）
- **カテゴリ**: データ / 操作
- **概要**: 反省エディタに折りたたみ3セクションを追加。①艇セッティング（全て数値）、②天候（風向/風速＋波高）、③反省内容（ラベル付き5欄）。
- **動機**: セッティング数値と狙い・課題・要因を構造化して残し、日々の走りと結びつけて振り返りたい。
- **メモ**: 新規反省を開くと直前の反省の艇セッティングを初期値にプリフィル＝微調整だけで済む。天候は日ごとに変わるので引き継がない。localStorage永続化。

---

## テーマ: セーリング反省アプリ化（内容を濃くする）
反省の流れを **ペアで反省 → ポジション別で反省 → 事務連絡** の3段で回し、
ペア／ポジション反省の内容を濃くすることを目標に据える。以下17〜はその実現に向けた機能群。

### 17. ホーム画面
- **ステータス**: ✅ 完了（起動時は `view-home`（body クラスで切替）。「新規練習 +」カードと保存済みプロジェクトをカード型グリッド `home-cards` に一覧表示、カードクリック→`openPractice`→`loadPractice`→`showTrack` で該当練習へ遷移。タイトルクリックでホームに戻る。`src/summary.js` の `practiceSummary` が保存JSONから軽量サマリ（トラック/反省/動画/イベント件数＋先頭風＋練習実データ時刻ラベル）を生成し、大きな points 配列に触れずカードへ表示、`cacheSummary` で localStorage キャッシュ即反映。表示日時はトラックGPS開始/動画配置時刻の最古＝`earliestContentMs` を JST で整形＝保存日ではなく“いつ練習したか”。フォルダ未選択時は選択ボタン、空時は空状態表示。TDDで `practiceSummary` を単体テスト）
- **関連改修**: 動画/反省パネルの開閉・セクション展開で `refitTransform` を外し `resizeCanvas` で w/h のみ更新＝pan/zoom(cx,cy,scale,rot)を保持し「今見ている領域」を維持。全体フィットは `showTrack`(ホーム→トラック遷移)のみに限定。
- **カテゴリ**: 操作 / 可視化
- **概要**: アプリ起点にホーム画面を作成。「新規 +」カードと過去の練習データをカード型で一覧表示し、カード選択でそのトラック画面へ遷移する。
- **動機**: いきなりトラック画面ではなく、練習日ごとに入口をまとめて選べるようにしたい。
- **メモ**: 過去練習の一覧ソース（保存済みプロジェクト `serializeProject`）。カードに前回の課題/発見サマリを載せると項目20と連動。ルーティング（ホーム⇄トラック）の実装方式。

### 18. iPhone13のmp4が動画挿入で弾かれる不具合
- **ステータス**: ✅ 完了（`videometa.js`: `findAppleCreationMs` に QuickTime `udta > date` アトム対応を追加。原因は「弾く」側でなく時刻復元の取りこぼしだった＝午後の`.MP4`は書き出しで `mvhd` creation_time が書き出し時刻(18:44 JST)に化け、本来の録画時刻は `com.apple.quicktime.creationdate` ではなく **`date` アトム(ASCII "date"+ISO8601)** に残っていた。現行は "creationdate" しか見ず取りこぼし→`apple:null`→mvhd(書出時刻)→GPS範囲外でスキップ。ASCII "date" の直後(先頭8B以内)にISO8601が続く場合のみ採用するフォールバックを追加＝誤検出防止。実機18本で午後14:14〜15:49 JSTに正しく復元、`.MOV`(午前)は回帰なし。TDDで単体テスト3件追加、全163テスト通過）
- **カテゴリ**: データ
- **概要**: iPhoneの`.MP4`（書き出しで撮影時刻が化けた午後の動画）が📁動画フォルダ取込で「範囲外」スキップされる問題を、`date`アトムから本来の録画時刻を復元して解消。
- **動機**: 端末により動画が添付できないと振り返りに使えない。
- **残る前提**: 時刻復元後も、動画を取り込むには**その時間帯のGPS軌跡を読み込んでおく**必要（範囲重なり判定のため）。午後動画には午後のGPSを、午前`.MOV`には午前のGPSを。

### 19. 反省のAI文章整形・参考文献提案
- **ステータス**: 💡
- **カテゴリ**: 解析 / データ
- **概要**: 反省で出てきた課題や発見に対し、手持ちのヨット技術書から該当する文献（サイト・文章・解説動画・プロ動画）を引っ張ってきて提案する。文章整形も補助。
- **動機**: 課題/発見に合った学習リソースへ自動で橋渡しし、反省を次の練習に繋げたい。
- **メモ**: Gemini等の無料で使えるAIの導入を想定。狙いは文章力よりアルゴリズム（課題→参考文献のマッチング）。技術書コンテンツのインデックス化方法（章立て・タグ付け）。項目16の構造化フィールド（課題/発見）を入力に使う。

### 20. 反省入力アプリ（個人が各自入力・時系列閲覧）
- **ステータス**: 💡
- **カテゴリ**: データ / 操作
- **概要**: 反省フォーマットを各自がアプリから入力し、練習データに保存。個人の反省を時系列で見返せる。ホーム画面には前回の課題と発見を表示。動画をドライブにアップロードするショートカットも用意。
- **動機**: 反省を各自が手元で入力・蓄積し、過去との連続性（前回課題→今回）を持って振り返れるようにしたい。
- **メモ**: 将来的にはアプリからトラックも見られるようにする。項目16の反省フォーマット／項目17のホーム画面と統合。個人別の保存・同期方式（localStorageを超える共有が要るか）。Driveアップロードは既存のローカル同期フォルダ運用（項目9メモ）と整合。

### 21. 反省中の録音・要約
- **ステータス**: 💡
- **カテゴリ**: データ / 解析
- **概要**: 反省中の会話を録音し要約する。議論の内容やぽろっと出た話を書き残す。
- **動機**: 口頭で出た気づきが記録に残らず流れてしまうのを防ぎたい。
- **メモ**: 録音→文字起こし→要約のパイプライン（無料AI活用は項目19と共通基盤化できるか）。要約を反省ノート（項目16）に紐付けて保存。

### 22. 全艇チューニングダッシュボード
- **ステータス**: ✅ 完了（反省の艇セッティング12項目を横断集計し、470船図中央に配置・期間バーで表示期間指定 src/tuning.js/dashboard.js/boatlayout.js/linechart.js/timebrush.js。適合点数・真後ろ画像比較は据え置き）
- **カテゴリ**: 解析 / 可視化
- **概要**: 反省で入力したチューニングデータを艇横断で比較検討するダッシュボード。反省にチューニングの適合点数を追加。各艇のクローズ中・真後ろからの画像を並べて比較できるようにする。
- **動機**: 艇ごとのセッティングと走りの良し悪しを見比べ、最適なチューニングを探りたい。
- **メモ**: データ源は項目16の艇セッティング12項目。「適合点数」の定義・入力UI。真後ろ画像の登録・時刻/艇との紐付け・並べ替え比較UI。

---

## 完了ログ
既存の実装済み機能（設計書 `2026-08-14-gps-track-viewer-design.md` 参照）:
CSV読込 / 外れ値除去 / 投影(fit) / 再生クロック / タイムライン(クロップ・スクラブ) /
複数トラック重ね(絶対・経過整列) / タグ(point/range・ピン) / pan/zoom
