# 芝工セイルログ

テーマ区分：SDGs × AI

— ヨット練習を「見返せる・比較できる・共有できる」記録に変えるAIサービス

> 【一行サマリ】
> ドライブに溜まる一方の練習・コーチング動画を、AIで自動整理し、「誰が・どの状況で・何を言われたか」を検索・比較・共有できる練習ログに変換します。コーチの言葉と選手の成長を、消えない資産にします。

---

## 1. 背景・課題

ヨット競技（セーリング）の練習では、コーチが伴走艇から選手へその場で指摘を出し、その様子をスマホなどで撮影しています。撮った動画はGoogle Driveなどに保存されますが、「撮って終わり」になっているのが実情です。

### 現場で起きている5つの痛み（部内ヒアリングから抽出）

| # | 課題 | 何が失われているか |
| --- | --- | --- |
| 1 | 溜まる一方で整理されない | ドライブに動画が大量に蓄積されるが分類されておらず、価値ある動画も埋もれます。見返す手間が大きく、結局ほとんど見返されません。 |
| 2 | 反復指摘に気づけない | 毎回の練習で「同じこと」を言われているのに、本人も指導側も気づけません。改善サイクルが回りません。 |
| 3 | 知見が横展開されない | ある部員が受けた有効なアドバイスが、同じ状況を通る他の部員に共有されません。学びが個人に閉じます。 |
| 4 | 速いセッティングが再現できない | リグのチューニングを日々変えているのに記録が残らず、「あの日速かったセッティング」を再現できません。 |
| 5 | 海上の音声が聞き取れない | 風・波・エンジンの騒音で、せっかくのコーチの指摘が録音上ほとんど聞き取れません。 |

### なぜ「AIでなければ解決できない」のか

強豪校は、圧倒的な練習量、1日2〜3時間に及ぶミーティング、そして専属コーチの質で差をつけています。これらを人の手で代用しようとすると、長時間のミーティングや専属コーチ、地道な手作業ログが必要になりますが、理工学系で授業の負担が大きい大学（週2日練習、休み期間は週5日の泊まり込み）では現実的に真似できません。

この「指導リソースの差」を、記録と分析の自動化で埋めるのがAIを使う必然性です。人が時間をかけないと得られなかった振り返り・比較・共有を、AIが撮影済みの動画から自動で生み出します。

### 社会的な広がり

指導リソースが限られた環境で、口頭コーチングと映像資産を最大限に活用するための仕組みです。「撮った動画が活用されない」「口頭指導が記録に残らない」という問題は、ヨットに限らず多くのマイナースポーツ・部活動に共通しています。

---

## 2. 想定ユーザーと利用シーン

- 選手：練習後、自分あての指摘だけを拾い読み。フォーム画像で走りを確認し、「前も同じことを言われている」に気づきます。
- コーチ：練習後にアップロードし、各選手のサマリと反復指摘を確認して、次回の指導計画に反映します。

利用主体は選手とコーチで、費用は部費から支払う想定です。開発・検証のモデルは芝浦工業大学体育会ヨット部です。これは「専属コーチがいない・全日本インカレに出場できていない・学連加盟」という、日本の多くの大学ヨット部を代表する現在の状態にあたります。ヨット部を持つ大学は日本に50弱あり、そのほぼすべてが練習中に動画を撮影しています。現時点でスケールは狙わず、まず芝浦で回して価値を確かめる方針です。

---

## 3. ソリューション概要

練習動画をアップロードすると、AIが自動で「クリーン化 → 文字起こし → 話者分離 → 状況タグ付け → フォーム画像抽出 → 要約」を行い、選手ごと・状況ごとに検索・比較・共有できる練習ログを生成します。

```
[練習動画(音声ノイズ入り)]
        │
        ▼
① 音声クリーン化 ── 実装済みの土台(DeepFilterNet3)
        │
        ├─▶ ② 文字起こし + ③ 話者分離  →「誰が・誰に・何を言ったか」
        ├─▶ ④ 状況タグ付け（風速/波/クローズ・ラン・リーチ/スタート/マーク回航）
        ├─▶ ⑤ 要約・反復指摘検出・成長管理（LLM）
        └─▶ 映像系（フォーム画像抽出/セールNo.識別/手ぶれ補正）※MVP後
        │
        ▼
[構造化された練習ログ]
  ・選手/艇ごとのタイムライン
  ・状況タグ × チューニングで検索
  ・過去練習との比較（反復指摘・速かったセッティングの検出）
  ・チーム内で知見を共有
```

---

## 4. 主要機能（課題との対応）

- 機能1「状況タグの自動付与」→ 課題1（整理されない）：波・風の状態と艇の走り方からタグを自動で割り振り、動画を検索できる資産に変えます。
- 機能2「文字起こし・話者分離・成長管理」→ 課題2・3・4（中核）：誰が誰に何を言ったかを記録・要約し、反復指摘を可視化。状況タグをキーに他部員のアドバイスも引けます。
- 機能3「映像系（フォーム画像・セールNo.識別・手ぶれ補正）」→ 課題1・2・3・4：フォームの比較や艇の識別、見返しやすい映像品質を後段で提供します。

---

## 5. AI技術要素の詳細仕様

実装状況は正直に区別しています。①は実データで稼働済み（設定は実測値）、②〜⑤は設計仕様が確定済みの実装予定です。

### ① 音声強調 — DeepFilterNet3 【実装済み・実測】

- サービス／モデル：DeepFilterNet3（deepfilternet 0.5.6、deepFilter CLI）
- 目的：風・波・エンジン音に埋もれたコーチの声を、後段のWhisperが認識できる明瞭さまで引き上げる（最難関の入口）
- 実行環境：PyTorch 2.2.2 / torchaudio 2.2.2、CPU実行（GPU不要）、macOS。処理速度は実測でリアルタイムの約16倍速
- 設定・条件（実測）：入力はffmpegで動画から分離しPCM 16bit WAVで抽出。DeepFilterNet3は内部48kHzで動作し、追加パラメータなしの標準設定で実行。出力はクリーン音声のみを映像に再結合
- 実績：Google Drive上の実練習動画23本（.MOV）を一括処理済み。rcloneで入出力を自動化し、処理済みファイルはスキップする再開可能設計
- 選定理由（比較検証あり）：初期はnoisereduce（スペクトルゲーティング）を試したが、非定常な海上騒音では音声が痩せた。深層学習ベースのDeepFilterNet3に切り替えて明瞭度が大きく改善したため採用

### ② 文字起こし（ASR）— faster-whisper large-v3 【設計確定・実装予定】

- サービス／モデル：faster-whisper（WhisperのCTranslate2高速実装）、モデルlarge-v3
- 目的：クリーン化した音声を、タイムスタンプ付きの日本語テキストに変換する
- 設定・条件：language="ja"に固定（言語誤判定の防止）、beam_size=5（精度重視）、VADフィルタ有効（無音・風音を除去し幻聴と処理時間を削減）、condition_on_previous_text=False（復唱ループの抑制）、word_timestamps=True（③との突き合わせに必須）、compute_typeはGPU時float16／CPU時int8。入力は必ず①のクリーン音声

### ③ 話者分離（Diarization）— pyannote.audio 3.1 【設計確定・実装予定】

- サービス／モデル：pyannote.audio 3.1（speaker-diarization-3.1）
- 目的：いつ・誰が話したかを区間分割し、②のテキストに話者を割り当てる
- 設定・条件：話者数ヒントはmin_speakers=2／max_speakers=4を想定。伴走艇側の支配的話者を「コーチ」、他を「選手」として初回に人手で対応付け、以降は声紋で自動継承。②の単語タイムスタンプと重なり最大で照合し「誰が・誰に・何を言ったか」を確定。入力は①のクリーン音声

### ⑤ 要約・反復指摘検出・成長管理 — Claude（Anthropic）【設計確定・実装予定】

- サービス／モデル：Claude（Anthropic）
- 目的：構造化した発話ログから、選手別サマリ・反復指摘の検出・状況別の知見抽出を行う
- 設定・条件：長文コンテキストに1セッション全体をまとめて投入。出力は構造化JSON（話者／宛先／指摘カテゴリ／状況タグ／要約）で受け取りDBへ格納。temperatureを低めにして再現性・一貫性を担保。反復指摘は過去指摘を文埋め込みで類似検索し、近い指摘をClaudeが「同一趣旨か」を判定して可視化

### 映像系・メタデータ系（構想段階 — 技術候補を明示）

| 領域 | 技術候補 | 目的 | 想定条件 |
| --- | --- | --- | --- |
| 映像CV | 物体検出＋向き/姿勢推定（YOLO系＋角度推定） | 真後ろ艇の瞬間を検出しフォーム画像を抽出、状況推定 | フレーム間引きで実時間処理、縦横比・向きで真後ろ判定 |
| 文字認識 | シーンOCR（セール番号検出） | セールNo.を読み取り艇（クルー）を識別 | 検出→切り出し→OCRの2段。低解像・傾き・水しぶきに拡張で対応 |
| 映像処理 | 電子式スタビライズ | 伴走艇の手ぶれ・波ブレ補正 | 特徴点トラッキングで動き推定→逆補正 |
| メタデータ連携 | GPS/時刻 × 気象オープンデータ | 風速・海況タグの自動補完 | 撮影時刻・位置を気象データと突合 |

技術的なポイントは、扱いにくい「口頭コーチング＋長時間映像」を、①で入口の音質を確保し、②③で構造化、⑤で意味づけする段階連結パイプラインです。各段が独立して差し替え可能なため、モデルの進化をそのまま品質向上に取り込めます。

---

## 6. 人の判断・検証プロセス（AIに任せきりにしない工夫）

AIの出力をそのまま使わず、人が判断・修正・検証する設計にしています。

- 音質の検証：noisereduceとDeepFilterNet3を実音声で聴き比べ、音声の痩せを人が判断してモデルを切り替えました。
- 誤りの抑制：VADフィルタやcondition_on_previous_text=Falseなど、AIが暴走しやすい海上音声の失敗パターンを設定で抑えています。
- 話者の対応付け：コーチと選手の初回ラベリングは人手で行い、以降を声紋で継承します。

---

## 7. 既存の実績・優位性

- 音声クリーン化パイプラインは既に稼働：Driveから練習動画を取得し、DeepFilterNet3でノイズを除去、映像に再結合してDriveへ書き戻す処理が実装済みで、実データ23本を処理済みです。「海上の音声が聞き取れない」という最難関の入口を、すでに解決しています。
- 実データを保有：実際の練習・コーチング動画を手元に持ち、開発初期からリアルなデータで検証できます。
- 当事者による開発：開発者自身が競技者であり、現場の課題を一次情報として理解しています。

---

## 8. ノベルティ（新規性）／競合調査から見えた立ち位置

セーリング分析ツールの大半は、GPSトラックを主入力とする「テレメトリ陣営」に属し、動画は同期表示にとどまり、口頭コーチングの音声はほぼ扱いません。

| サービス | 主入力 | 動画の扱い | 音声・口頭コーチング |
| --- | --- | --- | --- |
| kTool | GPSトラック | 同期表示のみ＋帆写真の形状計測(β) | 扱わない |
| OutSail.pro | テレメトリ | なし | 扱わない |
| SailViewer | GPSログ | なし | 扱わない |
| ChartedSails | GPSデータ | 同期表示のみ | 扱わない |
| SailingMetrics | GPX/VKX | CVで動画を自動分析 | 扱わない |
| SailSync | GPSトラック中心 | クラウド同期。音声を文字起こしに一部利用 | 付随的に扱う |

映像スタッツ抽出、口頭指導の構造化、音声検索、フォーム分析といった技術は他競技には既にありますが、セーリングには一つも降りていません。理由は「海上音声」で、風・波・エンジン音により屋外の音声系サービスが機能しないためです。

本サービスの新規性は、DeepFilterNet3による海上ノイズ除去を実装済みで、この壁を突破している点にあります。「証明済みの技術を、誰も入れなかった難所に初めて持ち込む」ことが独自の切り口です。

---

## 9. 実現可能性・開発ロードマップ

### ハッカソンで作るMVP（最小構成）

「クリーン動画 → 文字起こし → 話者分離 → 選手ごとの練習ログ・要約」までを1本通します。映像加工は後回しにし、実装済みのクリーン音声だけで中核の価値を単体で示します。

- 動画アップロード → 文字起こし＋話者分離
- 「誰が誰に何を言ったか」の一覧表示
- LLMによる練習サマリ＋反復指摘の簡易検出
- 状況タグは手動または簡易自動から開始

### その後の拡張

1. 状況タグの自動付与
2. 映像系（真後ろ艇の切り抜き、セールNo.識別、手ぶれ補正）
3. 選手/艇ごとの成長ダッシュボード／知見DB／チーム共有

### 効果の確認方法

反復指摘の検出精度や文字起こしの正確さを、コーチによる評価や正解書き起こしとの一致率で確かめます。まず芝浦工業大学ヨット部で運用し、価値を検証してから横展開を判断します。

---

## 10. 社会的インパクト・発展性

- 学生セーラー／地方・少人数クルー：専属アナリストがいなくても、自分たちの練習から最大限に学べます。
- コーチ・チーム：過去の指摘を記憶に頼らず参照でき、個人の学びがチームの資産として受け継がれます。
- 他競技への波及：「動画が活用されない」「口頭指導が残らない」はマイナースポーツ共通で、効率的なログの仕組みは横展開できます。

> 【まとめ】
> 消えていくコーチの言葉と、埋もれていく練習動画を、AIで「検索・比較・共有できる成長の記録」に変えます。指導リソースが限られた学生スポーツの現場から、口頭コーチング活用の新しい標準をつくります。
